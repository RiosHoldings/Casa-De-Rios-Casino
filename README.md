# Casino Casa de Ríos — Cloudflare Starter

Starter build for the future Casa de Ríos casino platform.

## Current files

- `index.html` — casino lobby
- `roulette.html` — roulette placeholder
- `styles.css` — purple/gold Casa de Ríos design
- `app.js` — temporary Player ID and backend test button
- `functions/api/health.js` — first Cloudflare Pages Function
- `schema.sql` — future D1 database tables

## First goal

Connect this repo to Cloudflare Pages and open the deployed site.

Then test:

`/api/health`

If it returns JSON saying the API is online, Cloudflare Pages Functions are working.
